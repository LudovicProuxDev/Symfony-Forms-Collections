{% extends 'base.html.twig' %}

{% block title %}
	{{ title }}
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>{{ title }}</h1>
		<p>
			<a href="{{ path('app_companies') }}">List of companies</a>
		</p>
		<p>
			<a href="{{ path('app_form_list') }}">Add company and teams with a list format</a>
		</p>
		<p>
			<a href="{{ path('app_form_table') }}">Add company and teams with a table format</a>
		</p>
	</div>
{% endblock %}
